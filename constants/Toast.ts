// MSG_HANDLER

export const TOAST_MSG_MAP = {
	create: { success: 'Board created', error: 'Failed to create board' },
	rename: { success: 'Board renamed', error: 'Failed to rename board' },
	copyLink: { success: 'Link copied', error: 'Failed to copy' },
	deletion: { success: 'Board deleted', error: 'Failed to delete board' }
}
